{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "PORT=${PORT:-3000} npx serve dist -l tcp://0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}